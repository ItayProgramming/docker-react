language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t ItayProgramming/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true itayprogramming/docker-react npm run test